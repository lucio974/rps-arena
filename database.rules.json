{
  "rules": {
    ".read": false,
    ".write": false,

    "_comment_presence": "Phase 2 will use presence/{uid} for live online status via onDisconnect(). Not used yet — locked down by default until that ships. When Phase 2 lands, this will become something like: presence/{uid} readable by any signed-in user, writable only by that uid.",

    "presence": {
      ".read": false,
      ".write": false
    }
  }
}
